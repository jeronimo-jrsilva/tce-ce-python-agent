services:
  api:
    build: .
    network_mode: "host"
    volumes:
      - .:/app
    env_file:
      - .env
    environment:
      - PYTHONUNBUFFERED=1
      - PYTHONPATH=/app
      - LLM_MODEL=${LLM_MODEL:-llama3.1}
      - OPENAI_API_BASE=${OPENAI_API_BASE:-http://localhost:11434/v1}
